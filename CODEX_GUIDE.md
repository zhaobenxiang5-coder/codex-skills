# 📘 Codex 实战进阶与使用心法：从新手到生产级 Agent 架构师指南

> **作者**：Zhuanz  
> **仓库**：[zhaobenxiang5-coder/codex-skills](https://github.com/zhaobenxiang5-coder/codex-skills)  
> **核心定位**：基于真实生产环境长期打磨沉淀的 Codex / AI Agent 落地方法论、架构心法、实战技巧与避坑全指南。

---

## 目录
- [第一篇：心法基石 —— Agent 协作的四大铁律](#第一篇心法基石--agent-协作的四大铁律)
- [第二篇：技能体系设计 —— 编写生产级 Skill 的黄金法则](#第二篇技能体系设计--编写生产级-skill-的黄金法则)
- [第三篇：浏览器与人机协同 —— Ego 双模协作实践](#第三篇浏览器与人机协同--ego-双模协作实践)
- [第四篇：跨模型协同 —— GPT 规划与 Codex 执行的最佳实践](#第四篇跨模型协同--gpt-规划与-codex-执行的最佳实践)
- [第五篇：四大高频业务领域实战沉淀](#第五篇四大高频业务领域实战沉淀)
- [第六篇：避坑大全 —— 常见陷阱与死循环根治](#第六篇避坑大全--常见陷阱与死循环根治)
- [第七篇：新手 5 分钟极速上手清单](#第七篇新手-5-分钟极速上手清单)

---

## 第一篇：心法基石 —— Agent 协作的四大铁律

在与 Codex、Claude Code 或 Antigravity 等自主智能体（Autonomous Agents）协同开发时，很多初学者常遇到“AI 瞎猜改坏代码”、“自言自语死循环”或“声称完成但实际没跑通”。要让 Agent 真正成为千军万马的研发副手，必须严格贯彻以下四大铁律：

### 铁律 1：先读现场再动笔（Read Before Act）
- **绝不让 Agent 凭空假设**：在任何修改或排查前，第一步永远是**读现有项目真实环境**（`list_projects`、检查配置文件、查看端口监听、查看最新日志）。
- **核实易变事实**：历史聊天记录或记忆中的文件名、PID、配置可能已经过时，必须在当前 Shell 中执行验证命令重新确认，不把旧对话当实时真理。
- **原文件原目录优先**：仅针对目标做局部精准修改，严禁全局推倒重写或随意创建临时平行文件。

### 铁律 2：业务完成 ≠ 技术成功（Business Acceptance != Tech Exit 0）
- **分清三个层次**：
  1. `尝试 (Attempt)`：命令已发出，或代码刚写入；
  2. `技术成功 (Technical Success)`：进程退出码为 0，或者网络请求返回 HTTP 200；
  3. `业务完成 (Business Completion)`：修改的代码实际达到预期业务目标，并经过**独立回读和结果验证**。
- **验证金句**：“HTTP 200、文件已保存、上传成功不等于业务验收”。Agent 必须在操作后，使用只读检查手段独立回读实际产生的数据或效果。

### 铁律 3：同目录唯一写入者原则（Single Writer Rule）
- 同一项目目录在同一时间内，**只能有一个 Agent 拥有写权限**。
- 如果同时运行两个有写权限的 Agent，极易产生文件锁竞争、代码互相覆盖和上下文撕裂。
- 其他辅助 Agent（如审查方、规划方）必须被限制为**只读模式**。

### 铁律 4：会话状态三态看板（Three-State Session Tracking）
在复杂任务长流程中，建议让 Agent 明确显示状态标记：
- `🌟 进行中`：当前正在推进的明确子步骤。
- `⏸️ 挂起等待`：等待人工介入（如扫码登录、人脸验证、涉及资金外发授权）或关键外部阻塞。
- `✅ 业务交付`：约定范围内的全流程验收完成，无未竟事宜。

---

## 第二篇：技能体系设计 —— 编写生产级 Skill 的黄金法则

Codex 的真正威力在于它的扩展技能（Skills）体系。一个规范的 Skill 能让 Agent 在特定业务场景下具备专家级水准。

### 1. 黄金目录结构解耦
一个高品质 Skill 应该清晰划分三种上下文角色：

```text
my-custom-skill/
├── SKILL.md              # 规范入口（必选，声明触发意图与流程契约）
├── scripts/              # 确定性脚本（Python/Node.js/Bash，负责脏活累活）
├── references/           # 领域长知识库（API 手册、校验清单、设计规范）
└── assets/               # 模板与素材资源（样板工程、图标、骨架代码）
```

- **`SKILL.md` 怎么写**：顶部必须包含标准 YAML Frontmatter：
  ```yaml
  ---
  name: 1688-product-search
  description: "在中国 1688 批发平台上搜索和分析商品，用于找货源、供应商发现和选品。当用户提到 1688 搜品、批发榜单时触发。"
  ---
  ```
- **核心心法：把“确定性逻辑”下沉到 `scripts/`**：
  不要让大模型在提示词里靠概率算数学题或拼凑复杂 API 鉴权，把参数签名、正则清洗、数据提取写成稳健的脚本，让 Agent 仅负责“意图识别与脚本参数调度”。

### 2. 避免大模型幻觉的上下文注入技巧
- **按需加载（Progressive Disclosure）**：主 `SKILL.md` 保持短小精悍（控制在 100-200 行以内），将细节放在 `references/` 下，引导 Agent 根据当前具体子阶段再读取对应 reference 文件，防止超长上下文稀释注意力。

---

## 第三篇：浏览器与人机协同 —— Ego 双模协作实践

纯无头（Headless）浏览器在处理验证码、滑块、风控和 SSO 登录时往往极易受挫。经过实践，**人机双模协同**是现代 Agent 浏览器自动化的最优解：

### 1. 复用用户日常环境
- 绝不重新启动一个完全干净无 Cookie 的空白无头浏览器去强行爬取。
- 利用协同工具（如 Ego-browser / Chrome 调试端口），直接复用人类已经登录的会话态，绕过繁琐的双重认证。

### 2. 独立任务空间（Task Space）隔离
- 每一个自动化任务分配独立的标签页分组或 Task Space。
- 操作结束后只清理本任务产生的临时标签页，绝不关闭用户正在工作或浏览的其他网页。

### 3. 敏感动作严禁越权
- **登录、扫码、手机验证码**：立即挂起并呼叫人类接管，等待用户完成后再恢复执行。
- **付款、对外发布内容、商务询盘发信**：必须输出详细预览信息，经用户明确敲定确认后方可点击发送。

---

## 第四篇：跨模型协同 —— GPT 规划与 Codex 执行的最佳实践

面对超大型工程或需要深思熟虑的架构重构，单一模型往往顾此失彼。最佳组合是 **“GPT 负责规划/审查 + Codex 负责落地执行”** 的双核协作模式：

```mermaid
flowchart LR
    User["用户需求"] --> GPT_Plan["ChatGPT 网页端
(架构规划 / 只读)"]
    GPT_Plan --> Codex_Exec["本机 Codex
(编码 / 调试 / 测试)"]
    Codex_Exec --> GPT_Review["ChatGPT 网页端
(独立回读 / 终审)"]
    GPT_Review -- "发现缺陷" --> Codex_Exec
    GPT_Review -- "验收通过" --> Delivery["交付成果"]
```

### 协作关键约定
1. **角色绝对清晰**：GPT 绝不直接操作本机写入或执行破坏性 Shell 命令；Codex 才是唯一的本机写入执行者。
2. **控制消息协议**：向 GPT 汇报时不粘贴海量源代码，只传递控制元数据与测试退出码：
   ```text
   [GPT-CODEX-COLLAB]
   TASK_ID: task-2026-09
   PHASE: REVIEW
   PROJECT: flow-engine
   MODIFIED: src/pipeline.ts, test/runner.test.ts
   TEST_RESULT: 12 passed, 0 failed
   ```
3. **最多 3 轮审查熔断**：为避免两个 AI 陷入“神仙打架”无限修改死循环，设定审查上限为 3 轮，若仍有分歧则保留证据并由人类开发者最终裁决。

---

## 第五篇：四大高频业务领域实战沉淀

### 1. 🛒 电商与供应链选品自动化
- **选品策略优先级**：`差异化现货` 永远优先于 `定制开发`。先跑自动化搜品筛选出已有现货买样测试，验证真实市场反馈；无合适款式才进入打样谈判。
- **接口容错设计**：Temu / 1688 / 店小秘 API 网关必须前置 `preflight` 预检，先校验 Token 与字段格式，再进行批量上传，杜绝因单条脏数据导致全批次失败。

### 2. 🎬 视频创作与动效渲染流水线
- **分层创意架构**：以 HyperFrames / Remotion 为代表的动效流水线，务必解耦为三层：
  1. `Core 契约层`：定义分辨率（如 1080x1920 竖屏）、帧率（30/60fps）与时间轴规范；
  2. `Animation 动效层`：原子级缓动曲线与镜头位移；
  3. `Creative 创意层`：台词剧本、色彩系统与字体质感。
- **草稿渲染提速**：开发调试时一律使用 `--quality draft` 或低分辨率快速出片，定稿验收时才开启完整高精度渲染。

### 3. 🎨 前端动效与高质感 UI 开发
- **性能红线**：GSAP 或 CSS 动效一律使用 `transform: translate3d/scale` 和 `opacity`，严禁在帧循环中直接改变 `top / left / width / height` 导致浏览器强制回流。
- **框架无泄漏生命周期**：在 React 中使用 `@gsap/react` 的 `useGSAP`，确保组件卸载时自动清理全部 Timeline 和 ScrollTrigger 监听。
- **拒绝 AI 油腻感（Anti-slop）**：坚决避开泛滥的千篇一律紫色霓虹、巨大圆角与无序渐变，坚持克制、留白与精致微交互。

### 4. 🛠️ 安全审计与逆向工程
- **静态先行，动态跟进**：逆向先用 `apktool` / `js-beautify` 提取分析架构与混淆结构，寻找关键加密函数入口后，再通过 Frida 进行最小范围 Hook。
- **沙盒与隔离执行**：所有未经验证的第三方脚本或样本，必须在独立隔离容器（如 Docker 或 Sandbox）中运行。

---

## 第六篇：避坑大全 —— 常见陷阱与死循环根治

| 典型翻车场景 | 根本原因 | 根治解决方案 |
| :--- | :--- | :--- |
| **Agent 陷入死循环**<br>（不停改同一处错） | 报错信息不完整，或每次报错后 Agent 只是盲目尝试随机修复 | 立即打断，要求 Agent 打印完整 Stacktrace，先复现最简用例，严禁盲目猜修。 |
| **代码越改越乱**<br>（破坏原有逻辑） | 每次改动都替换整个大文件，丢失原有注释与周围逻辑 | 强制使用局部补丁工具（`replace_file_content` / unified diff），每次仅替换目标代码块。 |
| **测试假阳性**<br>（明明没跑通却说完成了） | 测试脚本没有正确抛出非零退出码，或测试用例被弱化 | 必须验证命令退出码（`exit status`），并在测试中加入断言（Assert），以实际输出证据为准。 |
| **依赖环境冲突**<br>（本机 Python/Node 版本混乱） | 全局安装污染依赖，环境漂移 | 一律采用特定运行环境隔离（如 `npx`、`uvx`、`.venv`），不依赖脆弱的全局环境。 |
| **秘钥 Token 泄漏风险** | 在代码或文档中硬编码私有 Key | 采用统一环境变量注入，Git 提交前务必配置健全的 `.gitignore` 并进行正则扫描。 |

---

## 第七篇：新手 5 分钟极速上手清单

准备启动一个新项目时，对照以下五步清单快速对齐：

1. **[ ] 明确边界与工作目录**：让 Agent 确认目标绝对路径与 Git 分支，确认当前项目规则与限制。
2. **[ ] 挂载场景专属技能**：使用 `./install.sh --target <agent>` 提前挂载本仓库对应的领域 Skill（如 GSAP 动效、1688 搜品等）。
3. **[ ] 建立基线测试用例**：在修改前先跑通现有测试或构建命令，确保初始状态可编译。
4. **[ ] 小步快跑与实时提交**：每完成一个独立子特性立即运行验证，通过后保存 Git 阶段性提交。
5. **[ ] 终审独立回读验收**：任务收尾时由 Agent 给出产物绝对路径、验证日志与测试覆盖证据，由开发者最终确认。

---

*“好的程序员懂得写代码，卓越的 AI 时代工程师懂得驾驭 Agent。”*
